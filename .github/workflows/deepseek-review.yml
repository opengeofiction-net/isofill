name: Code Review
# pull_request, not pull_request_target. This repository is public, and
# pull_request_target runs with the base branch's permissions and access to
# secrets no matter who opened the pull request - which is the standard way
# CHAT_TOKEN gets exfiltrated. With pull_request, a branch in this repository
# still gets the secret and a fork does not, so a fork's pull request goes
# unreviewed rather than trusted. That is the safer half of the trade, and
# there are no forks.
on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

# fix: GraphQL: Resource not accessible by integration (addComment) error
permissions:
  pull-requests: write

jobs:
  setup-deepseek-review:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    name: Code Review
    steps:
      # The model does not know what day it is and reasons from its training
      # cutoff, so it has twice flagged correct 2026 dates as being in the
      # future and "likely a typo for 2025". Telling it costs one step.
      - name: Today
        id: today
        # %-d is GNU only; this format is portable
        run: echo "date=$(date -u '+%A %d %B %Y')" >> "$GITHUB_OUTPUT"

      - name: DeepSeek Code Review
        # pinned by digest: a tag can be moved, and this step holds a secret
        uses: hustcer/deepseek-review@74f086aea9d85827ea091cb1f6dbeeb54a0b8ad7 # v1.21
        with:
          chat-token: ${{ secrets.CHAT_TOKEN }}
          # pinned for the same reason the action is: this is the action's
          # current default, and a default that moves under us changes how
          # reviews read without anything here having changed
          model: deepseek-v4-flash
          temperature: 0.2
          exclude-patterns: 'pnpm-lock.yaml,package-lock.json,*.lock,*.tif'
          sys-prompt: >-
            You are reviewing a pull request against isofill, the C
            interpolator that turns rasterised contour lines into a digital
            elevation model for OpenGeofiction. One C file over the GDAL C
            API, OpenMP for the threads, a Makefile, and Debian packaging. It
            is consumed as a binary by the elevation build and by the Danu
            editor, and a golden test in the danu repository pins the surface
            it produces from a fixture at a named release, cell for cell.


            Today is ${{ steps.today.outputs.date }}; you cannot check this
            yourself, so take it from here rather than from what you remember.
            A date on or before today is not in the future. A date well beyond
            it still deserves reporting.


            Prioritise, in this order: memory safety and undefined behaviour
            - out-of-bounds indexing, integer overflow in size arithmetic,
            uninitialised reads, races between OpenMP threads; correctness of
            the interpolation, where a change that alters the output of the
            same input changes the published surface and must say so and bump
            the version, because the golden reference downstream will go red;
            packaging faults such as a dependency the Makefile or debian/
            control does not declare; and documentation or comments which
            contradict the code they describe.


            Commit messages, changelog entries and comments here are
            deliberately discursive and explain why a thing is the way it is.
            That is the house style. Do not report it as verbose, and do not
            suggest moving reasoning out of a commit message.


            You have no tools. There is no shell, no network and no checkout
            of the repository, and nothing will execute anything you emit. The
            diff and this prompt are all you get. Never write a tool call, a
            function call, or any markup that invokes one: nothing is listening,
            and it lands in the review comment as it stands.


            So "checked" means reasoned from the diff, not run. Prefer a few
            findings you can support that way to many you cannot. Say a thing
            the diff cannot settle only where it changes what the author should
            do about a finding; a list of what you would need in order to look
            further helps nobody, and the pull request thread may well already
            answer it.


            Reply with the review and nothing else. No preamble about what you
            are about to do, no narration of your reasoning as you go, and no
            reaching a conclusion and then reopening it - settle it first and
            report what you settled on. If the change is sound, say so briefly
            and stop.
          user-prompt: 'Review the following changes.'
